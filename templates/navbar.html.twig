{% set currentPath = path(app.request.attributes.get('_route'), app.request.attributes.get('_route_params')) %}
<nav class="navbar navbar-expand-sm navbar-dark bg-primary mb-3 nav">
    <div class="container">
        <div class="collapse navbar-collapse" id="mobile-nav">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item {{ currentPath == '/' ? 'active' : '' }}">
                    <a href="{{ path('exchange') }}" class="nav-link">Exchanger</a>
                </li>
                <li class="nav-item {{ '/transactions' in currentPath ? 'active' : '' }}">
                    <a href="{{ path('transactions') }}" class="nav-link">Transactions</a>
                </li>
            </ul>
        </div>
    </div>
</nav>
